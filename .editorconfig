# EditorConfig: Garante consistência entre diferentes editores
# 🎯 Analogia: Como um "padrão de formatação universal"

root = true

# Aplica para todos os arquivos
[*]
charset = utf-8
end_of_line = lf
indent_style = space
indent_size = 2
insert_final_newline = true
trim_trailing_whitespace = true

# Markdown permite espaços no final (para quebras de linha)
[*.md]
trim_trailing_whitespace = false
